/**
 * Auth route shell (`app/(auth)/*`): visual theme for logged-out users.
 *
 * - `true`  — light background, light card, dark text (default product expectation).
 * - `false` — cinematic dark shell (same palette as the marketing landing).
 */
export const AUTH_LAYOUT_USE_LIGHT_THEME = true;
